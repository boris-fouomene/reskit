import { ViewStyle } from "react-native";

export const absoluteClassName = "w-full h-full";
export const styles = {
    absoluteFill: {
        position: 'absolute',
        left: 0,
        right: 0,
        top: 0,
        bottom: 0,
        flex: 1,
    } as ViewStyle,
}