import { I18n } from "@resk/core";
import "./resources";

I18n.RegisterTranslations(({
    en: {
        "appBar": {
            title: "Resk Expo is the title",
            subtitle: "Resk Expo is the subtitle"
        }
    }
}));